export default function LandingPreviewLayout({ children }: { children: React.ReactNode }) {
  // Layout buit — la previsualització s'ha de mostrar sense sidebar ni header d'admin
  return <>{children}</>
}
